require 'pry'


def reformat_languages(languages)
  new_hash = {}
    languages.each do |language_name, language_attr|
        language_attr.each do |type, type_value|
          new_hash[:language_attr] = "ruby"



    #new_hash[:language] = "value"
    #new_hash[:language] = {attribute: "info"}

      end
    end
  end
